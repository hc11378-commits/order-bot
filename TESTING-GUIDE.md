# 機場接送 Bot 1.3.1 撤回封鎖版

本版本已完成自動化測試，但正式交給 30 多位司機前，仍必須在實際 LINE、Render、MongoDB 環境完成分階段驗收。外部服務的權限、網路與額度無法只靠本機測試完全模擬。

## 本版本已加強

- 每個 LINE 群組的訂單、取消與摘要完全隔離。
- 每筆新訂單額外保存匿名發送者代碼與群組類型，方便追查但不公開 LINE 原始 ID。
- 每次新增或更新訂單後，先從 MongoDB 同步該群組、該服務日的完整資料，重新部署後也不會只顯示最新一筆。
- 同一訂單編號重新貼上時更新內容與備註，不重複計數；改到新服務日時排除舊日期記錄。
- 缺少服務日不再自動存成今天；MongoDB 寫入失敗時會明確告知，不假裝儲存成功。
- 只有「客收」、沒有「結算價」的訂單仍會計入當日趟數；客收直接視為該訂單的已知業績，不顯示「待確認」。只有客收與結算價都沒有時才標示待確認。
- 結算價與客收分開顯示；業績總額固定以「結算價＋客收」計算，每日簡表、群組診斷與月結使用同一口徑。
- 無英數訂單編號的訂單使用 LINE 訊息 ID 區分，相同時間、地點與金額的兩趟不會互相覆蓋。
- 空白的「其他備註」不會跨行誤取聯絡人或電話，避免個資出現在簡表。
- 無「接機／送機」標題的自客單，只要有出發日期、乘車人數、上車地點及下車地點，仍視為訂單。
- `客收：900`、`客收900`、`結算價`與`結單價`皆可辨識；只有客收時，客收即為業績，不顯示待確認。
- 新版部署後收到的訂單會儲存 LINE 訊息 ID；客服使用 LINE「收回」時，僅取消原訊息建立的訂單，不影響其他群組。
- 客服收回無編號訂單後再貼修改版，舊訊息會取消、新訊息只計一趟；有訂單編號的修改版則直接更新同一筆。
- 輸入「訂單編號＋拉回改派」、「訂單編號＋改派」或「訂單編號＋收回」後，原單會退出更新簡表與月結，並留下禁止復活標記；舊 webhook 重送或再次貼上相同編號都不會重新加入。
- 同一群組多人同時操作時依序處理；不同群組仍可並行。
- Render 重新部署後，可從 MongoDB 找回舊訂單進行取消或改派。
- 日期固定儲存為 `M/D`，並另外保存年份，避免跨年度月結混在一起。
- 所有排程固定使用台灣時間 `Asia/Taipei`。
- `TEST_MODE=true` 時全面停用 Push，只使用 Reply API，避免消耗 Push 額度。
- 地址資料涵蓋全台 22 縣市、368 個鄉鎮市區，摘要顯示完整縣市與行政區。
- 缺少縣市且行政區重名時顯示「縣市待確認」，不再任意猜成台北。
- 改派時會先確認新訂單可辨識，才取消舊訂單。
- LINE 長訊息會安全分段；LINE API 設有逾時保護。
- 套件已有鎖定檔，安全掃描結果為 0 個已知弱點。

行政區清單核對來源：內政部國土測繪中心「代碼服務－鄉鎮市區清單（地政）」政府資料開放平臺。

## 上線前實際驗收順序

1. Render 環境變數保留 `TEST_MODE=true`。
2. 部署本 ZIP 內全部檔案。
3. Render Logs 必須同時看到 `MongoDB 連線成功` 與 `Server running`。
4. 開啟 `/health`，確認 `status` 為 `ok`、`databaseReady` 為 `true`、`version` 為 `1.3.1-withdrawal-lock`。
5. LINE Developers 執行 Webhook Verify，必須成功。
6. 在測試群組輸入「測試」，必須收到 `BOT 測試成功`。
7. 依序測試：單筆接機、單筆送機、多筆訂單、取消、改派、拉回、補單、交通車、月結。
8. 貼一筆沒有「接機／送機」標題、只有 `客收：900` 的自客單，確認顯示「結算價 0、客收 900、業績 900」，而且沒有「待確認」。
9. 在測試群組收回剛才的自客單，再貼修改版，輸入指定日期的群組診斷，確認只剩修改後的一趟。
10. 分別測試 `ABC12345 拉回改派`、`ABC12345 改派`、`ABC12345 收回`（編號請換成實際測試訂單），再重貼原訂單，確認 Bot 表示已忽略且簡表沒有重新加入。
11. 測試台北／新北、桃園、新竹縣／市、嘉義縣／市，以及一筆東部或離島地址。
12. 三個司機群組分別輸入 `群組診斷 9/10`（日期改成要核對的實際服務日），確認三個「群組代碼」彼此不同，並核對各群組的服務日訂單。
13. 舊式 `修正9/7`、`修正9/10` 等指令已停用；診斷完成前不得修改資料。
14. 先由 3～5 位司機試用一個完整工作日，核對 LINE 摘要與 MongoDB 訂單筆數。
15. 確認 LINE Push 額度足夠後，才把 `TEST_MODE` 改為 `false` 並逐步開放全體司機。

## 不可省略的人工情境

- 兩位司機幾乎同時在同群組貼單。
- 兩個不同群組同時貼不同訂單，確認摘要沒有互相混入。
- 部署後重啟服務，再取消部署前的訂單。
- 輸入不存在的訂單編號，確認 Bot 明確表示沒有變更資料。
- 改派訊息故意缺少新訂單欄位，確認舊訂單仍保留。
- 地址只寫「中正區」或「信義區」，確認顯示「縣市待確認」。
- 日期輸入 2/29（非閏年）、13/40 等錯誤內容，確認不會儲存。
- LINE 自動取消只適用於客服點選「收回」且 LINE 確實送達 unsend webhook 的訊息；只在手機端刪除訊息不會通知 Bot。部署本版以前的舊訂單沒有 LINE 訊息 ID，仍需用訂單編號取消或拉回。

## 正式開放條件

只有在自動化測試、LINE Verify、MongoDB 連線、測試群組完整情境及小規模試用全部通過後，才建議開放 30 多位司機使用。若任何一項不符，維持 `TEST_MODE=true` 並停止擴大使用。
